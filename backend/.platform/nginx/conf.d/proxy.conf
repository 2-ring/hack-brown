client_max_body_size 25M;

# SSE streaming: disable proxy buffering globally so event-stream
# responses are forwarded immediately.  This is safe for non-SSE
# requests too — Gunicorn already buffers its own responses.
proxy_buffering off;
