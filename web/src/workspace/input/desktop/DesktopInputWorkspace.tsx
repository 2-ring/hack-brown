import { DesktopDropArea } from './DesktopDropArea'
import type { BaseInputWorkspaceProps } from '../shared/types'

export function DesktopInputWorkspace(props: BaseInputWorkspaceProps) {
  return <DesktopDropArea {...props} />
}
